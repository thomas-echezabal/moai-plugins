---
name: inbox-assistant-schedule
description: Create, repair, or explain a recurring Inbox Assistant task in ChatGPT or Codex. Use when the user types /inbox-assistant:schedule, asks to schedule a daily brief, follow-through review, or owner reset, or needs a legacy scheduled task recreated safely.
---

# Inbox Assistant Schedule

Read `../../references/codex-compatibility.md` and `../../commands/schedule.md` completely, then follow the schedule command workflow. Treat any text after `/inbox-assistant:schedule` as the command argument.

The command file's `/schedule` heading and usage block are Claude's internal shorthand. Keep `/inbox-assistant:schedule` as the user-facing workflow name in ChatGPT and Codex. Use the current product's recurring automation only when it can retain the complete prompt and safety preamble; otherwise prepare the exact task proposal and state plainly that it was not scheduled.
